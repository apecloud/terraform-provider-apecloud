// Code generated by generator.py. DO NOT EDIT.

package datasource

{%- import "utils/schema_helper.j2" as schemaHelper %}

{%- set responseSchema = operations["singular"]["schema"]|get_schema_from_response %}
{%- set primitiveAttr, primitiveArrAttr, nonPrimitiveListAttr, nonPrimitiveObjAttr = responseSchema|tf_sort_properties_by_type %}

import (
	"github.com/hashicorp/terraform-plugin-framework/datasource/schema"
{%- if primitiveArrAttr or nonPrimitiveListAttr or nonPrimitiveObjAttr %}
	"github.com/hashicorp/terraform-plugin-framework/types"
{%- endif %}
)

func {{ name|camel_case }}DataSourceSchema() schema.Schema {
	return schema.Schema{
		Attributes: map[string]schema.Attribute{
            {%- for attrName, attrSchema in primitiveAttr.items() %}
            {{- schemaHelper.schemaPropertyBuilder(attrName, attrSchema, force_computed=True, is_request=False, parent_schema=operations["singular"]["schema"], is_datasource=True) }}
            {%- endfor %}

            {%- for attrName, attrSchema in primitiveArrAttr.items() %}
            {{- schemaHelper.schemaPropertyBuilder(attrName, attrSchema, force_computed=True, is_request=False, parent_schema=operations["singular"]["schema"], is_datasource=True) }}
            {%- endfor %}

            {%- for attrName, attrSchema in nonPrimitiveListAttr.items() %}
            {{- schemaHelper.schemaPropertyBuilder(attrName, attrSchema, force_computed=True, is_request=False, parent_schema=operations["singular"]["schema"], is_datasource=True) }}
            {%- endfor %}

            {%- for attrName, attrSchema in nonPrimitiveObjAttr.items() %}
            {{- schemaHelper.schemaPropertyBuilder(attrName, attrSchema, force_computed=True, is_request=False, parent_schema=operations["singular"]["schema"], is_datasource=True) }}
            {%- endfor %}
		},
	}
}