// Code generated by generator.py. DO NOT EDIT.

package resource

{%- import "utils/schema_helper.j2" as schemaHelper %}

{%- set createOperationSchema = operations[CREATE_OPERATION]["schema"] %}
{%- set createOperationParams = createOperationSchema|parameters %}
{%- set primitiveAttr, primitiveArrAttr, nonPrimitiveListAttr, nonPrimitiveObjAttr = createOperationParams|tf_sort_params_by_type %}

import (
	"github.com/hashicorp/terraform-plugin-framework/resource/schema"
{%- if primitiveArrAttr or nonPrimitiveListAttr or nonPrimitiveObjAttr %}
	"github.com/hashicorp/terraform-plugin-framework/types"
{%- endif %}
)

func {{ name|camel_case }}ResourceSchema() schema.Schema {
	return schema.Schema{
		Attributes: map[string]schema.Attribute{
			"id": schema.StringAttribute{
				Computed: true,
			},
            {%- for attrName, attrSchema in primitiveAttr.items() %}
            {{- schemaHelper.schemaPropertyBuilder(attrName, attrSchema, force_computed=False, is_request=True, parent_schema=createOperationSchema) }}
            {%- endfor %}

            {%- for attrName, attrSchema in primitiveArrAttr.items() %}
            {{- schemaHelper.schemaPropertyBuilder(attrName, attrSchema, force_computed=False, is_request=True, parent_schema=createOperationSchema) }}
            {%- endfor %}

            {%- for attrName, attrSchema in nonPrimitiveListAttr.items() %}
            {{- schemaHelper.schemaPropertyBuilder(attrName, attrSchema, force_computed=False, is_request=True, parent_schema=createOperationSchema) }}
            {%- endfor %}

            {%- for attrName, attrSchema in nonPrimitiveObjAttr.items() %}
            {{- schemaHelper.schemaPropertyBuilder(attrName, attrSchema, force_computed=False, is_request=True, parent_schema=createOperationSchema) }}
            {%- endfor %}
		},
	}
}